spring:
  application:
    name: backend
  datasource:
    url: jdbc:h2:file:./data/demo-db      # stores data in ./data/demo-db.mv.db file
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  error:
    include-stacktrace: never
    include-message: always
razorpay:
  keyId: rzp_test_RUq0uJckU4WH99
  keySecret: VmOy1526JxTiTsR2bQG8KAkl
  currency: INR
  appName: whistleup
sms:
  provider: fast2sms
  fast2sms:
    api:
      key: YOUR_API_KEY_HERE
  