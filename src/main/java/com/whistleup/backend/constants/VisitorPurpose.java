package com.whistleup.backend.constants;

/**
 * Visitor category for gate / lobby logs. JSON uses these enum names (must match app label map).
 */
public enum VisitorPurpose {
    FOOD_DELIVERY,
    PACKAGE_DELIVERY,
    RELATIVES_FRIENDS,
    OTHER
}
